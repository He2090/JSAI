# 项目整理记录

## 2026-06-19 本地版商用迭代目录整理

目标目录：

`C:\Users\admin\Desktop\最新版未测试\myJSAI\myJSAI\JSAI_LocalOnly`

本次整理目标是把该目录确定为后续商用迭代主目录，并剥离不应长期留在源码区的构建产物、缓存和临时文件。

已清理内容：

- 删除 `WinApp/bin`、`WinApp/obj`。
- 删除 `ClientInstaller/bin`、`ClientInstaller/obj`。
- 删除 `ClientConfigurator/bin`、`ClientConfigurator/obj`。
- 删除 `Updater/bin`、`Updater/obj`。
- 删除空旧发布目录 `_publish_out`。
- 删除空下载目录 `downloads`。
- 删除空发布产物目录 `artifacts`。
- 删除临时查看文件 `_tmp_projectlaunch_view.txt`。

保留内容：

- `WinApp`、`ClientInstaller`、`ClientConfigurator`、`Updater` 源码项目。
- `Workflowsapi` 本地 ComfyUI 工作流模板。
- `scripts` 构建脚本。
- `docs` 项目文档。
- `manifests` 更新清单。
- `JSAI.sln`、`CHANGELOG.md`、根目录配置/说明文件。

新增维护规则：

- 新增 `.gitignore`，忽略 `bin/obj`、发布产物、日志、缓存、用户本地模型配置、运行输出和项目生成资源。
- 后续安装包应统一从构建脚本或 `artifacts/installers` 产出，不把发布结果当作源码维护。

验证要求：

- 清理后需要重新执行 `dotnet build JSAI.sln -c Release`。
- 需要打包时再生成安装包，安装包和 payload 只作为发布物保存，不进入源码维护范围。

验证结果：

- 已执行 `dotnet build JSAI.sln -c Release`。
- 构建结果：0 个错误，99 个历史警告。
- 构建验证后，再次删除验证过程中重新生成的 `bin/obj`，保持源码目录干净。
- 当前目录未检测到有效 Git 仓库；后续商用迭代建议先建立正式版本管理。
